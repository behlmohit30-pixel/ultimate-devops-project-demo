# Apply with: kubectl create secret generic study-app-secrets \
#   --from-literal=anthropic-api-key=YOUR_KEY_HERE \
#   --dry-run=client -o yaml | kubectl apply -f -
#
# This file is a TEMPLATE only — do not store real keys here.
apiVersion: v1
kind: Secret
metadata:
  name: study-app-secrets
type: Opaque
stringData:
  anthropic-api-key: "REPLACE_WITH_REAL_KEY"
